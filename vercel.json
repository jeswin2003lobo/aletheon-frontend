{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "framework": "create-react-app",
  "rewrites": [
    { "source": "/((?!api|static|favicon|manifest|robots).*)", "destination": "/" }
  ]
}
